- Allow user to save weights of model quadruples, permanent directory split into 3 subdirectories, each with 4 files named after the model

- Allow to view summaries and plot architectures of models

- tickbox of which out of 4 models to plot/summarize, train, test etc.

- tickbox for every metric except accuracy

- tickbox for plotting loss, tickbox for plotting given metrics


LAYOUT:
 * select experiment combobox
 * 4 tickboxes for networks
 * group called metrics, tickbox for every metric
 * tickbox plot selected metrics
 * tickbox plot loss
 * epochs, batch_size
 * train selected models button
 * test selected models button


